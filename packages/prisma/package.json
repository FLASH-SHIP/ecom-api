{
  "name": "@ecom/prisma",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "postinstall": "prisma generate",
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "tsx seed.ts",
    "db:studio": "prisma studio",
    "clean": "rm -rf dist src/generated node_modules/.prisma"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@ecom/tsconfig": "workspace:*",
    "@types/pg": "^8.15.0",
    "dotenv": "^16.5.0",
    "prisma": "^7.8.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0"
  }
}

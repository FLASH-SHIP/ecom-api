# Future Work & Enhancements

- **Volumetric Dimensions**: Add `length`, `width`, `height`, and `maxWeight` limits to `PackingType` to automatically compute shipment volume weight.
- **Cost Calculation**: Associate a material cost with each packing type to track packaging expenses per order.
- **Inventory & Alerting**: Integrate packaging stock levels with alerts for low stock levels.
